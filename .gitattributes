
# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# Generated file — regenerated by pre-commit hook on every commit.
# On merge/rebase conflicts, keep our version; the hook overwrites
# it with fresh data on the next commit anyway.
backlog.html merge=ours
